Add the possibility to edit column config with a simple UI. Allow the user to edit column name, column decription (show it below the column name), add/remove columns. If a column was removed but the ticket belongs to it, the ticket will go into "undefined" column. Column preferences in the current project are transferred if the column is renamed.

