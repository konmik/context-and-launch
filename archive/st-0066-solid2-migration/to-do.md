We are going to migrate to solid2.

First we will need a plan, for that we need to find all relevant docs and examples, and migration guides.

The priority is to make the transition to the new framework as idiomatic to the new framework as possible. Do not try to keep the code similar to the existing, this is a wrong priority. The priority is matching the new framework idioms and ideal state of the architecture.

Since it is a technical automated ticket, it will not require any human input. The agent will have to make decisions on its own.
