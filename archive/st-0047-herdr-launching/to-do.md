We are currently running each agent in its own terminal, i want to run them inside herdr environment instead.

We need to investigate which changes we need to make to do it, maybe we need to change default settings or launching script, ENTER keyword or whateer.

I want to launch each project in its own herdr environment.
