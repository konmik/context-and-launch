Currently all CLI commands are hardcoded in the app.
If the user wants to review them or modify to their workflow it is impossible.
Make a json config with map of commands, llow the user to edit these commands in settings as templates.
Do not allow the user to add or remove commands, the list is fixed.
Add a button to reset to default each command.