Add a tab in settings to configure apps, they must be configured using templates.

Add a tab to ticket window, to launch other apps against the branch. (IDE, git diff, and other commands)

Show a list of configured apps, with run button ">" icon. When user clicks on the button it would just launch the tempalte.

Lift "launch in worktree" toggle above layers.
