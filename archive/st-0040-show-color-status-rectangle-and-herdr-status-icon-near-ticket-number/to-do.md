I wan to show a rectangle with ticket status after ticket number so that in tree view it would be obvious at what stage the ticket is.
Also allow to assign a color to each ticket state in settings where columns are edited.

Also show the status of each ticket's agent in herdr as a corresponding icon.