Each item it projects dropdown must have a button with a square and arrow, clicking it the project would open in a new window instead of reusing the current one. We must adapt the server code to serve many windows at the same time. The goal of the feature is to be able to switch between window  instead of constantly toggling the current project. Mutliple windows also make it easier to work with several desktops.